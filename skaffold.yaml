apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/cccaternberg/example-maven-api:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/cccaternberg/example-maven-api:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: example-maven-api
        chartPath: charts/example-maven-api
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/cccaternberg/example-maven-api"
          image.tag: "{{.TAG}}"
